import { StudentProgress } from "@/components/student-progress";

export default function LearningPage() { return <div className="space-y-5"><StudentProgress /><section className="rounded-xl border border-border bg-card p-5"><p className="font-mono text-[10px] font-bold uppercase tracking-[0.18em] text-indigo-400">Your local workflow</p><div className="mt-3 grid gap-3 sm:grid-cols-3">{[["01", "Clone", "craft clone kv-store"], ["02", "Build", "Use your own IDE, tests, and git workflow."], ["03", "Push", "Receive incremental stage feedback after git push."]].map(([number, title, description]) => <div key={number} className="rounded-lg bg-accent p-4"><p className="font-mono text-indigo-400">{number}</p><p className="mt-2 font-mono text-xs font-bold uppercase">{title}</p><p className="mt-1 text-xs text-zinc-500">{description}</p></div>)}</div></section></div>; }
